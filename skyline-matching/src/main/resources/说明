该服务组件时用于币币交易撮合，目前只是配置单个节点。
增加节点时，
 
 
 方案1：可根据行情进行区分。例如：
   行情id为：1，2，3，--->节点：“trade_code_1”;
   行情id为 :4,5,6 --->   节点："trade_code_2"
  ....
 注意：
 
     mq采用路由模式即可 
          并且修改加载数据项，对应的节点行情数据，确保数据一致。
 
 
 
 方案2：（单个节点）可根据行情进行分，开启不同线程进行撮合，
     注意：数据一致性